on:
  workflow_dispatch:

name: DEV-Deploy to Azure
jobs:
  login-and-deploy-DEV:
    runs-on: ubuntu-latest
    environment: dev
    steps:
      # Checkout code dev   
    - name: checkout
      uses: actions/checkout@main

      # Log into Azure
    - name: login
      uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

      # Deploy Bicep file
    - name: DEV:deploy to azure
      uses: azure/arm-deploy@v1
      with:
        deploymentName: dev-deployment
        scope: subscription
        region: 'westeurope'
        subscriptionId: ${{ secrets.AZURE_SUBSCRIPTION }}
        resourceGroupName: ${{ secrets.AZURE_RG }}
        template: ./main.bicep
        parameters:  environment=dev location=northeurope
        failOnStdErr: false

  login-and-deploy-PROD:
    needs: login-and-deploy-DEV
    runs-on: ubuntu-latest
    environment: prod
    steps:
      # Checkout code dev   
    - name: checkout
      uses: actions/checkout@main

      # Log into Azure
    - name: login
      uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

      # Deploy Bicep file
    - name: PROD-WHAT-IF:deploy to azure
      uses: azure/arm-deploy@v1
      with:
        deploymentName: prod-deployment
        scope: subscription
        region: 'westeurope'
        subscriptionId: ${{ secrets.AZURE_SUBSCRIPTION }}
        resourceGroupName: ${{ secrets.AZURE_RG }}
        template: ./main.bicep
        parameters:  environment=prod location=westeurope
        additionalArguments: "--what-if --rollback-on-error --what-if-exclude-change-types Create Ignore"
        failOnStdErr: false
    
              # Deploy Bicep file
    - name: PROD:deploy to azure
      uses: azure/arm-deploy@v1
      with:
        deploymentName: prod-deployment
        scope: subscription
        region: 'westeurope'
        subscriptionId: ${{ secrets.AZURE_SUBSCRIPTION }}
        resourceGroupName: ${{ secrets.AZURE_RG }}
        template: ./main.bicep
        parameters:  environment=prod location=westeurope
        failOnStdErr: false